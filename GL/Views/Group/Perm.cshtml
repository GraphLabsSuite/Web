@model List<GL.Models.PermissionInGroup>

@{
    ViewBag.Title = "Управление правами";
}

<h2>Управление правами @ViewBag.Name</h2>

@using (Html.BeginForm()) {
      @Html.ValidationSummary(true)
<fieldset>

    <legend>@ViewBag.Name</legend>

<table>
    <tr>
        <th></th>
        <th>
            Описание
        </th>
        <th></th>
    </tr>

@for(int i=0; i<Model.Count; i++) {
    <tr>
        <td>
            @Html.CheckBoxFor(modelItem => modelItem[i].Enable, new { id = "box_"+i.ToString() })
        </td>
        <td>
            @Html.DisplayFor(modelItem => modelItem[i].Name)
        </td>
        <td>
        @Html.HiddenFor(modelItem => modelItem[i].ID)
        @Html.HiddenFor(modelItem => modelItem[i].ID_Permission)
        @Html.HiddenFor(modelItem => modelItem[i].Name)
        </td>
    </tr>
}

</table>
    <p>
        <input type="button" value="Снять выделение" onclick="fillall( @Model.Count , false )" />
        <input type="button" value="Отметить все" onclick="fillall( @Model.Count , true )" />
        <input type="button" value="Стандартные права" onclick="standard()" />
    </p>
    <p>
        <input type="submit" value="Сохранить" />
    </p>
</fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    
    <script type="text/javascript">
        function fillall(count, value) {
            for (var i = 0; i < count; i++) {
                document.getElementById('box_' + i.toString()).checked = value;
            }
        }

        function standard()
        {
            alert('not implemented yet :(');
        }
    </script>
}