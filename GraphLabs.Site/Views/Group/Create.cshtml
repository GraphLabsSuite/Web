@model GraphLabs.DataModel.Group

@{
    ViewBag.Title = "Создание группы";
}

<h2>Создание группы</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IsRegistrationAvailbale)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IsRegistrationAvailbale)
            @Html.ValidationMessageFor(model => model.IsRegistrationAvailbale)
        </div>

        <p>
            <input type="submit" value="Создать" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Обратно к списку", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
